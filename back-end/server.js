import {exec} from 'child_process';

function ConsultarVeiculo(placa) {
	return new Promise((resolve, reject) => 
	const comando = mysql -u root -p 123 -D auto_check -e 
		"SELECT * from Veiculo WHERE placa = '${placa}';

	exec(comando
	}
}
		(error,stdout ,stderr) =>